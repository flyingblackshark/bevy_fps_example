use bevy::prelude::*;
use bevy::ecs::event::{Events, ManualEventReader};
use bevy::input::mouse::MouseMotion;

pub struct ShootingSystem;

impl Plugin for ShootingSystem {
    fn build(&self, app: &mut App) {

    }
}